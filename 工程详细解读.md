# ZeroClaw 工程详细解读

## 项目概述

ZeroClaw 是一个用 100% Rust 编写的 AI 助手基础设施项目，旨在提供零开销、零妥协的运行时操作系统。它是一个为代理工作流设计的运行时基础设施，抽象了模型、工具、内存和执行，使代理可以构建一次并在任何地方运行。

### 核心特性

- **极低资源占用**: 在 $10 硬件上运行，内存占用 < 5MB，比 OpenClaw 节省 99% 的内存
- **快速启动**: 冷启动时间 < 10ms，二进制文件大小约 8.8MB
- **跨平台架构**: 支持 ARM、x86 和 RISC-V 架构
- **完全可交换**: 核心系统都是 traits（提供商、渠道、工具、内存、隧道）
- **安全优先**: 配对、严格沙箱、显式允许列表、工作区范围
- **无锁定**: OpenAI 兼容的提供商支持 + 可插拔的自定义端点

### 性能对比

| 指标 | OpenClaw | NanoBot | PicoClaw | ZeroClaw 🦀 |
|------|----------|---------|----------|-------------|
| 语言 | TypeScript | Python | Go | **Rust** |
| RAM | > 1GB | > 100MB | < 10MB | **< 5MB** |
| 启动时间 (0.8GHz) | > 500s | > 30s | < 1s | **< 10ms** |
| 二进制大小 | ~28MB | N/A | ~8MB | **~8.8 MB** |
| 成本 | Mac Mini $599 | Linux SBC ~$50 | Linux Board $10 | **Any hardware $10** |

## 项目结构

```
zeroclaw/
├── src/                    # 主要 Rust 源代码
│   ├── agent/             # AI 代理核心逻辑
│   ├── providers/         # AI 模型提供商集成
│   ├── channels/          # 通信渠道集成
│   ├── tools/             # 可调用的工具
│   ├── memory/            # 记忆系统
│   ├── security/          # 安全策略
│   ├── config/            # 配置管理
│   ├── runtime/           # 运行时管理
│   ├── observability/     # 可观测性
│   ├── hardware/          # 硬件支持
│   ├── peripherals/       # 外设支持
│   ├── gateway/           # Web 网关
│   ├── cron/              # 定时任务
│   ├── hooks/             # 钩子系统
│   ├── skills/            # 技能系统
│   ├── sop/               # 标准操作流程
│   └── tunnel/            # 隧道支持
├── web/                   # React 前端界面
├── python/                # Python 工具包
├── crates/                # 独立的 Rust crates
│   └── robot-kit/         # 机器人工具包
├── firmware/              # 各种硬件的固件
│   ├── arduino/           # Arduino 固件
│   ├── esp32/             # ESP32 固件
│   ├── esp32-ui/          # ESP32 UI 固件
│   ├── nucleo/            # STM32 Nucleo 固件
│   └── uno-q-bridge/      # Arduino Uno Q 桥接
├── docs/                  # 文档
├── tests/                 # 测试
├── fuzz/                  # 模糊测试
├── benches/               # 性能基准测试
├── dev/                   # 开发工具
├── examples/              # 示例代码
└── scripts/               # 构建和部署脚本
```

## 核心模块详解

### 1. Agent 模块 (src/agent/)

AI 代理的核心逻辑，负责处理消息、调用工具、管理对话历史。

**主要组件**:
- `agent.rs`: 代理主实现
- `loop_.rs`: 消息处理循环
- `dispatcher.rs`: 消息分发器
- `classifier.rs`: 查询分类器
- `memory_loader.rs`: 记忆加载器
- `prompt.rs`: 提示词构建

**关键功能**:
- 消息处理和工具调用循环
- 对话历史管理
- 记忆集成
- 查询分类和路由

### 2. Providers 模块 (src/providers/)

AI 模型提供商的抽象和实现，支持多种 LLM 提供商。

**支持的提供商**:
- OpenAI (包括 OpenAI Codex)
- Anthropic Claude
- Google Gemini
- Ollama (本地推理)
- OpenRouter
- Bedrock (AWS)
- Copilot (GitHub)
- Telnyx
- GLM (智谱)
- 以及其他兼容 OpenAI API 的提供商

**关键组件**:
- `traits.rs`: Provider trait 定义
- `reliable.rs`: 可靠性包装器，支持故障转移
- `router.rs`: 模型路由
- 各提供商的具体实现

**特性**:
- 统一的 API 接口
- 自动重试和故障转移
- OAuth 认证支持
- 流式响应支持

### 3. Channels 模块 (src/channels/)

多渠道消息传递基础设施，连接 ZeroClaw 到外部平台。

**支持的渠道**:
- CLI (命令行界面)
- Discord
- Telegram
- Slack
- WhatsApp (包括 WhatsApp Web)
- Matrix (支持 E2EE)
- Signal
- Mattermost
- Nextcloud Talk
- Lark/Feishu
- DingTalk
- IRC
- Nostr
- QQ
- Email (IMAP/SMTP)
- iMessage
- ClawdTalk (自定义协议)

**关键组件**:
- `traits.rs`: Channel trait 定义
- 各渠道的具体实现
- 消息处理和格式化
- 健康检查和重连机制

**特性**:
- 统一的消息接口
- 并发消息处理
- 对话历史管理
- 指示器支持（正在输入等）

### 4. Tools 模块 (src/tools/)

代理可调用的工具集合，提供各种功能。

**内置工具**:
- **文件操作**: `file_read`, `file_write`, `file_edit`, `glob_search`
- **Shell 执行**: `shell`, `screenshot`
- **Web 操作**: `web_fetch`, `web_search_tool`, `http_request`, `browser`, `browser_open`
- **记忆管理**: `memory_store`, `memory_recall`, `memory_forget`
- **定时任务**: `cron_add`, `cron_list`, `cron_remove`, `cron_run`, `cron_update`
- **Git 操作**: `git_operations`
- **PDF 处理**: `pdf_read`
- **图像处理**: `image_info`
- **代理委托**: `delegate`
- **配置管理**: `model_routing_config`, `proxy_config`
- **通知**: `pushover`
- **硬件工具**: `hardware_board_info`, `hardware_memory_map`, `hardware_memory_read` (需要硬件特性)
- **SOP 管理**: `sop_advance`, `sop_approve`, `sop_execute`, `sop_list`, `sop_status`

**关键组件**:
- `traits.rs`: Tool trait 定义
- `schema.rs`: 工具模式定义
- 各工具的具体实现

**特性**:
- JSON Schema 参数验证
- 异步执行
- 安全策略执行
- 可扩展的工具注册

### 5. Memory 模块 (src/memory/)

记忆系统，支持多种后端和向量搜索。

**支持的后端**:
- SQLite (默认)
- Markdown
- Lucid (本地 + SQLite)
- PostgreSQL (需要特性)
- Qdrant (向量数据库)
- None (禁用记忆)

**关键组件**:
- `traits.rs`: Memory trait 定义
- `backend.rs`: 后端分类和配置
- `embeddings.rs`: 嵌入向量生成
- `chunker.rs`: 文本分块
- `hygiene.rs`: 记忆清理
- `vector.rs`: 向量搜索

**特性**:
- 多种存储后端
- 向量搜索支持
- 自动清理
- 响应缓存
- 快照支持

### 6. Security 模块 (src/security/)

安全基础设施，提供策略执行、沙箱和密钥管理。

**主要组件**:
- `policy.rs`: 安全策略定义
- `secrets.rs`: 加密密钥存储
- `pairing.rs`: 设备配对
- `audit.rs`: 审计日志
- `estop.rs`: 紧急停止
- `prompt_guard.rs`: 提示注入防护
- `leak_detector.rs`: 信息泄漏检测
- `domain_matcher.rs`: 域名匹配
- `otp.rs`: OTP 验证

**沙箱后端**:
- Docker
- Firejail (Linux)
- Bubblewrap (Linux)
- Landlock (Linux)
- Noop (无沙箱)

**特性**:
- 自主级别控制
- 工作区边界
- 访问控制规则
- OS 级隔离
- 审计日志

### 7. Config 模块 (src/config/)

配置管理系统，定义所有配置结构。

**主要配置**:
- `Config`: 主配置结构
- `AgentConfig`: 代理配置
- `ChannelsConfig`: 渠道配置
- `MemoryConfig`: 记忆配置
- `SecurityConfig`: 安全配置
- `RuntimeConfig`: 运行时配置
- `ObservabilityConfig`: 可观测性配置
- `HardwareConfig`: 硬件配置
- `PeripheralsConfig`: 外设配置
- 各渠道的具体配置

**特性**:
- TOML 配置文件
- 环境变量支持
- 配置验证
- JSON Schema 生成

### 8. Runtime 模块 (src/runtime/)

运行时管理，支持多种执行环境。

**支持的运行时**:
- Native (原生)
- Docker
- WASM (WebAssembly)

**关键组件**:
- `traits.rs`: Runtime trait 定义
- 各运行时的具体实现

**特性**:
- 统一的运行时接口
- 资源限制
- 沙箱集成

### 9. Observability 模块 (src/observability/)

可观测性系统，提供日志、指标和追踪。

**主要组件**:
- `log.rs`: 日志记录
- `prometheus.rs`: Prometheus 指标
- `otel.rs`: OpenTelemetry 集成
- `runtime_trace.rs`: 运行时追踪
- `verbose.rs`: 详细输出

**特性**:
- 结构化日志
- 指标导出
- 分布式追踪
- 多后端支持

### 10. Gateway 模块 (src/gateway/)

Web 网关，提供 HTTP API 和 WebSocket 支持。

**主要组件**:
- `api.rs`: REST API
- `ws.rs`: WebSocket 处理
- `sse.rs`: Server-Sent Events
- `static_files.rs`: 静态文件服务

**特性**:
- RESTful API
- 实时通信
- 前端资源嵌入

### 11. Cron 模块 (src/cron/)

定时任务系统，支持周期性任务执行。

**主要组件**:
- `scheduler.rs`: 调度器
- `schedule.rs`: 任务调度
- `store.rs`: 任务存储
- `types.rs`: 类型定义

**特性**:
- Cron 表达式支持
- 任务持久化
- 执行历史

### 12. Hardware 模块 (src/hardware/)

硬件支持，包括设备发现和交互。

**主要组件**:
- `discover.rs`: 设备发现
- `introspect.rs`: 设备内省
- `registry.rs`: 设备注册表

**特性**:
- USB 设备枚举
- 串口通信
- 设备信息查询

### 13. Peripherals 模块 (src/peripherals/)

外设支持，包括各种硬件外设。

**支持的外设**:
- Arduino (上传和通信)
- STM32 Nucleo (编程和内存读取)
- Raspberry Pi GPIO
- Uno-Q 桥接

**主要组件**:
- `arduino_flash.rs`: Arduino 固件烧录
- `arduino_upload.rs`: Arduino 上传
- `nucleo_flash.rs`: Nucleo 编程
- `rpi.rs`: Raspberry Pi 支持
- `serial.rs`: 串口通信

### 14. Hooks 模块 (src/hooks/)

钩子系统，允许在特定事件时执行自定义逻辑。

**内置钩子**:
- `command_logger`: 命令日志记录

**主要组件**:
- `traits.rs`: Hook trait 定义
- `runner.rs`: 钩子运行器

**特性**:
- 事件驱动
- 可扩展的钩子注册

### 15. Skills 模块 (src/skills/)

技能系统，提供高级功能。

**主要组件**:
- `audit.rs`: 技能审计
- `symlink_tests.rs`: 符号链接测试

### 16. SOP 模块 (src/sop/)

标准操作流程系统，支持结构化工作流。

**主要组件**:
- `engine.rs`: SOP 引擎
- `dispatch.rs`: SOP 分发
- `gates.rs`: SOP 门控
- `condition.rs`: 条件评估
- `metrics.rs`: SOP 指标

**特性**:
- 结构化工作流
- 条件执行
- 批准流程
- 执行状态跟踪

### 17. Tunnel 模块 (src/tunnel/)

隧道支持，用于远程访问。

**支持的隧道**:
- Cloudflare
- Ngrok
- Tailscale
- Custom (自定义)
- None (无隧道)

**特性**:
- 安全远程访问
- 多隧道提供商
- 自动配置

## Web 前端 (web/)

基于 React 的 Web 界面，提供可视化的管理界面。

**技术栈**:
- React 19
- TypeScript
- Vite
- Tailwind CSS
- React Router

**主要页面**:
- Dashboard: 仪表板
- AgentChat: 代理聊天
- Config: 配置管理
- Memory: 记忆管理
- Tools: 工具管理
- Logs: 日志查看
- Cost: 成本追踪
- Cron: 定时任务
- Integrations: 集成管理
- Doctor: 诊断工具

**特性**:
- 实时通信 (WebSocket/SSE)
- 响应式设计
- 国际化支持

## Python 工具包 (python/)

Python 伴侣包，提供 LangGraph 集成。

**主要模块**:
- `agent.py`: 代理实现
- `tools/`: 工具实现
  - `base.py`: 基础工具
  - `file.py`: 文件工具
  - `memory.py`: 记忆工具
  - `shell.py`: Shell 工具
  - `web.py`: Web 工具
- `integrations/`: 集成
  - `discord_bot.py`: Discord 机器人

**依赖**:
- LangGraph >= 0.2.0
- LangChain Core >= 0.3.0
- LangChain OpenAI >= 0.2.0
- httpx >= 0.25.0

**特性**:
- LangGraph 集成
- 一致的 LLM 代理执行
- 工具调用支持

## Robot Kit (crates/robot-kit/)

完整的 AI 机器人工具包，专为 Raspberry Pi 部署设计。

**主要工具**:
- `drive`: 全向移动（前进、横移、旋转）
- `look`: 摄像头捕获 + 视觉模型描述
- `listen`: 语音转文本（Whisper.cpp）
- `speak`: 文本转语音（Piper TTS）
- `sense`: LIDAR、运动传感器、超声波距离
- `emote`: LED 表情和音效

**硬件要求**:
- Raspberry Pi 4 (4GB) 或 Pi 5
- USB 摄像头
- USB 麦克风
- 带功放的扬声器
- 电机控制器（L298N、TB6612 等）
- 4 个直流电机 + 全向轮

**软件依赖**:
- Ollama (本地 LLM)
- Whisper.cpp (语音转文本)
- Piper TTS (文本转语音)
- ROS2 (可选，用于高级机器人)

**特性**:
- 离线推理支持
- 实时语音交互
- 视觉感知
- 运动控制

## 固件支持 (firmware/)

### Arduino (firmware/arduino/)
- 基础 Arduino 固件

### ESP32 (firmware/esp32/)
- ESP32 微控制器固件
- Rust 嵌入式开发
- 支持自定义配置

### ESP32 UI (firmware/esp32-ui/)
- 带 UI 的 ESP32 固件
- 使用 Slint UI 框架

### Nucleo (firmware/nucleo/)
- STM32 Nucleo 开发板固件
- Rust 嵌入式开发

### Uno-Q Bridge (firmware/uno-q-bridge/)
- Arduino Uno Q 桥接固件
- Python 桥接程序
- Google Cloud Platform 集成

## 文档 (docs/)

完整的文档系统，支持多语言。

**主要文档类别**:
- **Getting Started**: 入门指南
- **Project**: 项目信息
- **Contributing**: 贡献指南
- **Reference**: 参考文档
  - API 参考
  - CLI 命令参考
  - SOP 参考
- **Operations**: 运维指南
- **Security**: 安全文档
- **Hardware**: 硬件文档
  - 数据手册
  - 设置指南
- **Setup Guides**: 设置指南
- **Maintainers**: 维护者文档

**支持的语言**:
- 英语
- 中文（简体）
- 日语
- 越南语
- 其他 20+ 种语言

## 测试 (tests/)

集成测试和端到端测试。

**主要测试**:
- WhatsApp Webhook 安全测试
- Telegram 集成测试
- 提供商解析测试
- 记忆比较测试
- 钩子集成测试
- OpenAI Codex Vision E2E 测试
- 记忆重启测试
- 等等

## 模糊测试 (fuzz/)

模糊测试目标，用于发现安全漏洞。

**主要目标**:
- 命令验证
- 配置解析
- 提供商响应
- 工具参数
- Webhook 载荷

## 性能基准测试 (benches/)

代理性能基准测试。

**主要基准**:
- 代理基准测试

## 开发工具 (dev/)

开发环境配置和工具。

**主要组件**:
- CI/CD 配置
- Docker 配置
- 配置模板
- 脚本工具

## 示例 (examples/)

示例代码和集成示例。

**主要示例**:
- 自定义渠道
- 自定义记忆
- 自定义提供商
- 自定义工具

## 技术栈

### 核心技术
- **语言**: Rust 2021 Edition
- **运行时**: Tokio (异步运行时)
- **HTTP 客户端**: Reqwest
- **HTTP 服务器**: Axum
- **序列化**: Serde
- **配置**: TOML
- **日志**: Tracing

### 前端技术
- **框架**: React 19
- **语言**: TypeScript
- **构建工具**: Vite
- **样式**: Tailwind CSS
- **路由**: React Router

### Python 工具包
- **框架**: LangGraph
- **LLM 集成**: LangChain
- **HTTP 客户端**: httpx

### 机器人技术
- **本地 LLM**: Ollama
- **语音转文本**: Whisper.cpp
- **文本转语音**: Piper TTS
- **机器人框架**: ROS2 (可选)

### 数据库
- **SQLite**: 默认记忆后端
- **PostgreSQL**: 可选记忆后端
- **Qdrant**: 向量数据库

### 安全
- **加密**: ChaCha20Poly1305
- **HMAC**: SHA-256
- **沙箱**: Docker, Firejail, Bubblewrap, Landlock

### 可观测性
- **日志**: Tracing
- **指标**: Prometheus
- **追踪**: OpenTelemetry

## 构建和部署

### 构建要求

**Windows**:
- Visual Studio Build Tools
- Rust toolchain

**Linux/macOS**:
- Build essentials
- Rust toolchain

### 构建命令

```bash
# 构建发布版本
cargo build --release

# 构建特定特性
cargo build --release --features hardware

# 构建前端
cd web
npm install
npm run build

# 构建 Python 包
cd python
pip install build
python -m build
```

### 部署选项

1. **单机部署**: 直接运行二进制文件
2. **Docker 部署**: 使用 Docker 容器
3. **服务部署**: 使用 systemd 或其他 init 系统
4. **云部署**: 部署到云平台

## 配置

ZeroClaw 使用 TOML 配置文件，默认位置为 `~/.zeroclaw/config.toml`。

**主要配置项**:
- 代理配置（模型、温度等）
- 渠道配置（Discord、Telegram 等）
- 记忆配置（后端、嵌入等）
- 安全配置（自主级别、沙箱等）
- 运行时配置（Docker、Native 等）
- 可观测性配置（日志、指标等）
- 硬件配置（设备、外设等）

## 安全特性

1. **配对机制**: 设备配对用于渠道认证
2. **沙箱隔离**: 多种沙箱后端支持
3. **访问控制**: 基于自主级别的访问控制
4. **审计日志**: 记录安全相关事件
5. **提示注入防护**: 防止提示注入攻击
6. **信息泄漏检测**: 检测敏感信息泄漏
7. **加密存储**: 密钥加密存储

## 扩展性

ZeroClaw 设计为高度可扩展：

### 添加新提供商
1. 实现 `Provider` trait
2. 在 `create_provider` 中注册
3. 添加配置结构

### 添加新渠道
1. 实现 `Channel` trait
2. 在 `start_channels` 中注册
3. 添加配置结构

### 添加新工具
1. 实现 `Tool` trait
2. 在 `all_tools` 中注册
3. 添加参数 Schema

### 添加新沙箱
1. 实现 `Sandbox` trait
2. 在 `create_sandbox` 中注册
3. 添加配置支持

## 社区和支持

- **GitHub**: https://github.com/zeroclaw-labs/zeroclaw
- **官网**: https://zeroclawlabs.ai
- **文档**: https://github.com/zeroclaw-labs/zeroclaw/tree/main/docs
- **贡献指南**: CONTRIBUTING.md
- **行为准则**: CODE_OF_CONDUCT.md

## 许可证

双重许可证：
- MIT License
- Apache License 2.0

## 贡献者

由哈佛、MIT 和 Sundai.Club 社区的学生和成员构建。

## 总结

ZeroClaw 是一个功能完整、性能优异的 AI 助手基础设施项目。它采用 Rust 编写，具有极低的资源占用和快速的启动时间，支持多种 AI 模型提供商、通信渠道和工具。项目架构清晰，模块化设计良好，易于扩展和维护。无论是用于个人项目还是企业应用，ZeroClaw 都提供了一个强大而灵活的 AI 助手运行时平台。

项目的核心优势在于：
1. **性能优异**: 极低的内存占用和快速的启动时间
2. **架构清晰**: 模块化设计，职责分离
3. **可扩展性强**: 基于 trait 的设计，易于添加新功能
4. **安全优先**: 多层安全防护
5. **跨平台**: 支持多种硬件和操作系统
6. **社区活跃**: 活跃的开发社区和完善的文档

ZeroClaw 代表了 AI 助手基础设施的一个新方向，专注于性能、安全性和可扩展性，为构建下一代 AI 应用提供了坚实的基础。